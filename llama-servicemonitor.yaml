apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: llama-cpu
  namespace: inference
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: llama-cpu
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
